import { FC } from "react";
// import { IBtn } from "./Btn.d";
// import styles from "./Btn.module.scss";

const Btn: FC = () => {
  return (
    <button type={'button'}>
      Кнопка
    </button>
  );
};

export default Btn;
